# 《上班挂机模拟器》完整产品设定文档

---

## 一、产品概述

### 1. 产品名称

**上班挂机模拟器**（Working AFK Simulator）

### 2. 产品定位

一款将“日常上班行为”游戏化的轻量级挂机模拟产品，通过数值增长、状态反馈和成就系统，帮助上班族降低心理负担、提升掌控感，并获得持续的正反馈。

### 3. 核心理念

> 你不是在上班，你是在挂机刷数值。

产品不强调效率压榨，而是通过**可视化 + 情绪反馈**，将不可控的上班过程转化为可感知、可结算、可截图传播的“进度条”。

---

## 二、目标用户

### 1. 核心用户

- 互联网 / 创意 / 技术岗位上班族
- 长期面对需求变更、任务碎片化的打工人
- 喜欢数据反馈、进度条、成就系统的用户

### 2. 使用场景

- 上班打开浏览器 / 小程序放在后台
- 工作间隙查看“挂机进度”

---

## 三、世界观设定

### 1. 世界观背景

你身处一个名为「工位大陆」的世界。 时间会自动流逝，任务会不断刷新，而你唯一能做的就是——

> **保持在线，稳定输出，避免崩盘。**

### 2. 玩家身份

- 身份：打工人
- 初始职业：见习上班族
- 可进阶职业：
  - 稳定输出者
  - 提效工程师
  - 需求调律师
  - 世界线修复师

---

## 四、核心系统设计

### 1. 角色系统

#### 1.1 角色基础属性

| 属性       | 描述             |
| ---------- | ---------------- |
| 等级 Lv    | 反映长期上班经验 |
| 经验 XP    | 用于升级         |
| 精力 EN    | 反映精神状态     |
| 专注 FOCUS | 影响挂机效率     |
| 贡献 CP    | 用于解锁内容     |

#### 1.2 状态规则

- 精力随在线时间缓慢下降
- 精力过低会触发负面状态
- 合理休息可恢复精力并获得 Buff

---

### 2. 挂机系统（核心）

#### 2.1 挂机定义

只要满足条件，系统即自动进入挂机状态，无需用户主动操作。

#### 2.2 挂机触发条件（可配置）

- 页面在线 ≥ X 分钟
- 工作时段内
- 浏览器 / App 活跃

#### 2.3 挂机结算逻辑

每个结算周期（如 10 分钟）：

```
+ XP（基础值）
+ CP（概率获得）
- 精力
```

挂机效率受以下因素影响：

- 当前精力
- 专注 Buff
- 是否激活 AI 协助

---

### 3. 任务系统

#### 3.1 任务类型

| 现实行为 | 游戏化任务      |
| -------- | --------------- |
| 修 Bug   | 🐞 清剿异常节点 |
| 改需求   | 🔄 世界线修复   |
| 对需求   | 📜 剧情对话     |
| 写文档   | 📚 知识遗迹记录 |

#### 3.2 任务特性

- 不强制用户填写真实内容
- 可一键完成（象征性）
- 主要作用：提供情绪反馈和奖励

---

### 4. Buff 系统

#### 4.1 Buff 类型

| Buff        | 效果         |
| ----------- | ------------ |
| 🤖 AI 协助  | XP 获取 +30% |
| ☕ 咖啡因   | 精力下降减缓 |
| ⚡ 提效工具 | 挂机效率提升 |
| 📘 总结沉淀 | 额外 CP      |

#### 4.2 Buff 获取方式

- 手动点击
- 成就解锁
- 特定时间段自动触发

---

### 5. 成就系统

#### 5.1 成就分类

- 时间类（连续挂机）
- 状态类（低精力生存）
- 行为类（使用 AI / 总结）
- 黑色幽默类（反内耗）

#### 5.2 成就示例

```
🏆《连续 7 天稳定挂机》
🏆《需求没有再次变更》
🏆《今日未崩盘》
```

成就卡片支持截图传播。

---

## 五、情绪反馈系统（灵魂）

### 1. 实时文案提示

根据当前状态随机生成提示：

- 🟢「你正在稳定输出中……」
- 🟡「世界线出现轻微扰动」
- 🔴「警告：精力接近临界值」

### 2. 下班结算文案

```
【今日挂机结算】
在线时长：6h 40m
经验获取：+86
状态评价：尚未崩盘
```

---

## 六、反内耗与保护机制

- 精力过低时，XP 获取递减
- 长时间高负荷触发“燃尽警告”
- 主动休息获得长期正向 Buff

产品明确传达：

> **休息是系统允许的最优解之一。**

---

## 七、与 TAPD 集成的系统设计（核心扩展）

> 本章节在不改变产品“情绪支持型挂机模拟器”定位的前提下，引入 TAPD 作为 **数据来源与事件触发器**，实现“真实工作行为 → 游戏反馈”的弱绑定。

---

### 7.1 设计原则（非常重要）

1. **只读、不评估、不考核**

   - TAPD 数据仅用于触发反馈与结算
   - 不做 KPI、排名、对比

2. **弱映射、强情绪**

   - TAPD ≠ 真实效率
   - TAPD 事件只作为“世界事件”的一种来源

3. **用户可控**

   - 所有 TAPD 绑定项可手动开关
   - 明确告知：不会展示给管理者

---

### 7.2 TAPD 接入范围（建议）

#### 7.2.1 接入方式

- 使用 TAPD Open API
- OAuth / Token 方式授权（仅个人维度）
- 不需要管理员权限

#### 7.2.2 推荐接入对象

| TAPD 对象       | 用途            |
| --------------- | --------------- |
| 需求（Story）   | 世界事件 / 剧情 |
| 缺陷（Bug）     | 副本 / 战斗     |
| 任务（Task）    | 日常任务        |
| 状态变更        | 结算触发        |
| 评论 / 变更记录 | 文案素材        |

---

### 7.3 TAPD → 游戏系统映射设计

#### 7.3.1 需求系统映射

| TAPD 需求状态 | 游戏化表现    |
| ------------- | ------------- |
| 新建          | 📜 新剧情出现 |
| 规划中        | 🧭 世界线推演 |
| 实现中        | ⚙ 世界线执行  |
| 已完成        | ✨ 剧情收束   |
| 状态回退      | 🔄 世界线震荡 |

触发效果：

- 获得 XP
- 触发「需求变更」世界事件文案

---

#### 7.3.2 Bug 系统映射

| TAPD Bug 行为 | 游戏反馈        |
| ------------- | --------------- |
| 新增 Bug      | 🐞 异常节点生成 |
| 修复          | 🗡 异常清除      |
| 重开          | ☠ Boss 复活     |

奖励逻辑：

- 修复 Bug = XP + CP
- Bug 重开 = 情绪文案 + 成就判定

---

#### 7.3.3 任务系统映射

| TAPD Task 行为 | 游戏化      |
| -------------- | ----------- |
| 新建任务       | 📌 日常委托 |
| 完成任务       | ✅ 委托结算 |

说明：

- 不展示任务真实内容
- 仅显示任务数量与类型

---

### 7.4 挂机结算 + TAPD 事件融合

#### 7.4.1 结算优先级

```
TAPD 事件触发 > 时间挂机结算 > 手动行为
```

#### 7.4.2 示例结算逻辑

```txt
[10:30]
检测到 Bug 状态变更：已解决
→ 触发副本结算
→ XP +12
→ CP +2
→ 文案："异常节点已清除，世界线趋于稳定"
```

---

### 7.5 世界事件系统（TAPD 驱动）

TAPD 的「不可控性」被包装为世界事件。

#### 7.5.1 世界事件示例

| 触发条件         | 世界事件      |
| ---------------- | ------------- |
| 多个需求同日变更 | 🌪 需求风暴    |
| Bug 短时间激增   | 🧨 系统不稳定 |
| 需求回退         | 🌀 世界线回滚 |

世界事件只影响：

- 文案
- UI 氛围
- Buff / Debuff

不影响真实工作。

---

### 7.6 成就系统（TAPD 专属）

#### 7.6.1 示例成就

```
🏆《需求三连变仍未崩盘》
🏆《Bug 修复但又回来了》
🏆《状态回退免疫体》
```

成就名称**不出现 TAPD 字样**，仅隐喻。

---

### 7.7 数据与隐私策略

- 所有 TAPD 数据：
  - 本地缓存
  - 只存 ID / 状态 / 时间
- 不上传服务器（默认）
- 支持一键解绑 + 清空

---

## 八、产品形态与技术建议

### 8.1 产品形态

- H5 / 小程序 / uni-app
- 支持 PC 后台运行

### 8.2 技术实现建议

- 前端定时轮询 TAPD 接口
- 或 Webhook → 本地事件触发
- 本地状态机管理游戏状态

---

## 九、TAPD 接口最小调用清单（MVP）

> 本章节用于 **Electron 桌面挂件版本** 的最小可行实现，仅覆盖「个人需求 + Bug 榜单 + 状态变更检测」，不涉及任何绩效、工时、他人数据。

---

## 9.1 接入前置说明

### 9.1.1 鉴权方式（推荐）

- TAPD Open API
- **Personal Token / Access Token**
- 只读权限

Electron 建议：

- Token 存储在本地（加密 / keychain）
- 不上传服务器

---

## 9.2 API 基础配置

```txt
Base URL:
https://api.tapd.cn

Headers:
Authorization: Bearer {ACCESS_TOKEN}
Content-Type: application/json
```

---

## 9.3 接口一览（最小集）

| 模块 | 接口         | 用途         |
| ---- | ------------ | ------------ |
| 用户 | 获取当前用户 | 获取 user_id |
| Bug  | 查询我的 Bug | Bug 榜单     |
| 需求 | 查询我的需求 | 需求榜单     |

---

## 9.4 获取当前用户信息

### 接口说明

用于确认当前 Token 所属用户，仅调用一次即可缓存。

### 请求

```http
GET /users/current
```

### 参数

无

### 示例返回

```json
{
  "id": "123456",
  "name": "张三",
  "email": "zhangsan@company.com"
}
```

前端仅需保存：

```ts
{
  id, name;
}
```

---

## 9.5 查询「指派给我的」Bug 列表

### 接口说明

用于生成 Bug 榜单，并检测状态变更。

### 请求

```http
GET /bugs
```

### 请求参数（MVP）

| 参数        | 说明      | 示例                           |
| ----------- | --------- | ------------------------------ |
| assigned_to | 指派人 ID | 123456                         |
| limit       | 条数限制  | 50                             |
| fields      | 返回字段  | id,title,status,updated_at,url |

### 示例请求

```http
GET /bugs?assigned_to=123456&limit=50&fields=id,title,status,updated_at,url
```

### 示例返回

```json
[
  {
    "id": "BUG-101",
    "title": "页面白屏",
    "status": "已解决",
    "updated_at": "2025-01-15T10:30:00",
    "url": "https://tapd.cn/bug/BUG-101"
  },
  {
    "id": "BUG-102",
    "title": "按钮无响应",
    "status": "新建",
    "updated_at": "2025-01-15T09:20:00",
    "url": "https://tapd.cn/bug/BUG-102"
  }
]
```

---

## 9.6 查询「指派给我的」需求（Story）列表

### 接口说明

用于生成需求榜单和需求状态分布。

### 请求

```http
GET /stories
```

### 请求参数（MVP）

| 参数        | 说明      | 示例                           |
| ----------- | --------- | ------------------------------ |
| assigned_to | 指派人 ID | 123456                         |
| limit       | 条数限制  | 50                             |
| fields      | 返回字段  | id,title,status,updated_at,url |

### 示例请求

```http
GET /stories?assigned_to=123456&limit=50&fields=id,title,status,updated_at,url
```

### 示例返回

```json
[
  {
    "id": "STORY-201",
    "title": "订单列表优化",
    "status": "实现中",
    "updated_at": "2025-01-15T11:00:00",
    "url": "https://tapd.cn/story/STORY-201"
  },
  {
    "id": "STORY-202",
    "title": "国际化支持",
    "status": "规划中",
    "updated_at": "2025-01-14T17:45:00",
    "url": "https://tapd.cn/story/STORY-202"
  }
]
```

---

## 9.7 前端统一数据模型（建议）

```ts
interface TapdItem {
  id: string;
  type: "bug" | "story";
  status: string;
  updatedAt: string;
  url: string;
}
```

---

## 9.8 状态变更检测（Diff 逻辑）

### 本地缓存结构

```ts
Record<id, status>;
```

### Diff 示例

```ts
if (prev[item.id] && prev[item.id] !== item.status) {
  triggerWorldEvent(item);
}
```

---

## 9.9 轮询策略（Electron 推荐）

| 项目     | 配置     |
| -------- | -------- |
| 轮询间隔 | 5 分钟   |
| 前台     | 正常轮询 |
| 后台     | 降频     |
| 网络异常 | 静默失败 |

---

## 9.10 明确不接入的 TAPD 接口

- 工时 / 工时统计
- 成员列表
- 项目统计
- 需求完成率
- 任何管理视图接口

---

## 十、桌面挂件 UI 规格说明（Electron 版）

> 本规格用于指导 **《上班挂机模拟器》桌面挂件** 的 UI 设计与实现，目标是： **常驻、不打扰、一眼即懂、随时可点。**

---

## 10.1 设计原则

1. **低存在感但高可读性**

   - 不抢焦点
   - 不遮挡主工作区域
   - 允许“被忽略”，但随时可查看

2. **信息密度克制**

   - 默认只展示「数量 + 状态」
   - 不展示复杂列表

3. **一切可跳转**

   - 所有条目点击后： → 唤起浏览器 → 打开对应 TAPD 页面

---

## 10.2 挂件窗口规格

### 10.2.1 窗口类型

- Electron 无边框窗口（frameless）
- Always on Top（可关闭）
- 不在任务栏显示

---

### 10.2.2 默认尺寸（折叠态）

```txt
宽度：260 ~ 300 px
高度：140 ~ 160 px
```

推荐初始尺寸：

```txt
280 × 150 px
```

---

### 10.2.3 展开态尺寸（可选）

```txt
宽度：300 ~ 360 px
高度：260 ~ 320 px
```

展开态用于展示：

- 状态分组详情
- 今日状态变更记录（简要）

---

## 10.3 折叠态 UI 布局（核心）

```txt
┌────────────────────────┐
│ 上班挂机模拟器  Lv.12  │
│ ───────────────────── │
│ 🐞 Bug   3｜今日变更 1 │
│ 📜 需求  5｜进行中 2  │
│ ───────────────────── │
│ 🟢 稳定挂机中  3h20m  │
└────────────────────────┘
```

### 区块说明

#### 1️⃣ 顶部状态栏

- 产品名 / 等级
- 可拖拽区域

#### 2️⃣ 榜单区（重点）

- Bug / 需求两行
- 左：总数
- 右：关键状态数量（如“今日变更”）

#### 3️⃣ 挂机状态栏

- 当前挂机文案
- 今日累计挂机时间

---

## 10.4 展开态 UI 布局（进阶）

```txt
┌────────────────────────────┐
│ 上班挂机模拟器 Lv.12   ×  │
│ ───────────────────────── │
│ 🐞 Bug 状态分布           │
│ 新建 1｜处理中 1｜已解决 1│
│                            │
│ 📜 需求状态分布           │
│ 规划中 2｜实现中 2｜完成 1│
│                            │
│ 📅 今日世界事件           │
│ - 异常节点已清除          │
│ - 世界线轻微震荡          │
└────────────────────────────┘
```

说明：

- 默认不自动展开
- 用户点击标题或状态区展开

---

## 10.5 交互行为规范

### 10.5.1 点击行为

| 区域               | 行为                  |
| ------------------ | --------------------- |
| Bug 行             | 打开 Bug 列表（TAPD） |
| 需求行             | 打开需求列表（TAPD）  |
| 单个状态（展开态） | 打开对应筛选页        |
| 顶部标题           | 展开 / 折叠           |

---

### 10.5.2 Hover 与反馈

- Hover 时高亮整行
- 显示提示：
  - "点击查看 TAPD"

---

## 10.6 文案与状态规范

### 10.6.1 挂机状态文案（示例）

| 状态     | 文案              |
| -------- | ----------------- |
| 正常     | 🟢 稳定挂机中     |
| 有变更   | 🟡 世界线轻微扰动 |
| 高频变更 | 🔴 系统不稳定     |
| 下班后   | 🌙 挂机暂停       |

---

## 10.7 颜色与视觉风格

- 背景：深色半透明（适配桌面）
- 字体：系统默认
- 颜色仅用于状态区分
- 避免高饱和

---

## 10.8 行为边界（必须遵守）

- ❌ 不弹系统通知（默认）
- ❌ 不强制打断工作
- ❌ 不自动展开
- ✅ 所有提示为被动可见

---

## 十一、世界事件 & 文案生成器规则

> 本章节定义《上班挂机模拟器》中 **世界事件（World Event）系统** 与 **文案生成器（Copy Generator）** 的完整规则，用于将 TAPD 的状态变化与时间流逝，转译为低压、有情绪价值的反馈。

---

## 11.1 设计目标

1. **给变化一个解释，而不是评价**
2. **减少不可控带来的心理噪音**
3. **让用户感觉“系统在陪你一起承受”**

核心原则：

> 不说“你效率低”，只说“世界线在震荡”。

---

## 11.2 世界事件分类

世界事件分为三大类：

| 分类        | 来源               | 是否依赖 TAPD |
| ----------- | ------------------ | ------------- |
| ⏱ 时间事件  | 在线时长 / 精力    | ❌            |
| 🔄 状态事件 | Bug / 需求状态变化 | ✅            |
| 🌪 聚合事件  | 多次变化统计       | ✅            |

---

## 11.3 时间类世界事件（基础挂机）

### 11.3.1 触发规则

| 条件              | 事件             |
| ----------------- | ---------------- |
| 连续挂机 ≥ 30 min | 稳定运行         |
| 连续挂机 ≥ 2 h    | 长时间输出       |
| 连续挂机 ≥ 4 h    | 高负荷运转       |
| 下班时间到        | 世界进入低活跃期 |

### 11.3.2 示例文案池

- 🟢「系统稳定运行中……」
- 🟢「时间正在缓慢流逝，一切如常」
- 🟡「长时间运行，注意能量消耗」
- 🌙「世界进入低活跃状态」

---

## 11.4 状态变更事件（TAPD 驱动）

### 11.4.1 Bug 状态事件

| 触发条件   | 世界事件     | 情绪权重 |
| ---------- | ------------ | -------- |
| Bug 新增   | 异常节点生成 | 中       |
| Bug 已解决 | 异常节点清除 | 正       |
| Bug 重开   | Boss 复活    | 负（轻） |

#### 文案示例

- 🐞「检测到新的异常节点」
- 🗡「异常节点已清除，世界线趋于稳定」
- ☠「异常节点重新活跃，但尚可控制」

---

### 11.4.2 需求状态事件

| TAPD 状态变化 | 世界事件     |
| ------------- | ------------ |
| 新建          | 新剧情出现   |
| 实现中        | 世界线推进   |
| 已完成        | 剧情阶段收束 |
| 状态回退      | 世界线回滚   |

#### 文案示例

- 📜「新的剧情分支被写入世界」
- ⚙「世界线正在执行中」
- ✨「一个阶段的故事暂告段落」
- 🔄「世界线发生回溯，但未脱轨」

---

## 11.5 聚合型世界事件（高级）

> 用于描述“不是你的问题，而是环境波动”。

### 11.5.1 触发规则示例

| 条件                    | 世界事件      |
| ----------------------- | ------------- |
| 同日 ≥ 3 次需求状态变更 | 🌪 需求风暴    |
| Bug 重开 ≥ 2            | ☠ 不稳定异常  |
| Bug + 需求同时变更      | 🌀 世界线震荡 |

---

### 11.5.2 聚合文案示例

- 🌪「世界线受到外部扰动，正在自适应」
- 🌀「多重变量叠加，系统短暂波动」
- 🧯「异常频发，但仍在安全阈值内」

---

## 11.6 文案生成器规则（核心）

### 11.6.1 文案结构模板

```txt
[情绪图标] + [系统视角描述] + [稳定性暗示]
```

示例：

> 🟡 世界线轻微震荡，但结构尚稳定

---

### 11.6.2 文案生成流程

```txt
事件触发
 → 计算事件类型
 → 判断情绪权重
 → 从对应文案池随机
 → 输出一条当前态文案
```

---

### 11.6.3 情绪权重规则

| 权重       | 使用比例 | 说明         |
| ---------- | -------- | ------------ |
| 正向       | 40%      | 强化成就感   |
| 中性       | 50%      | 日常陪伴     |
| 负向（轻） | 10%      | 共情，不施压 |

> ❗ 禁止出现强负向、指责型文案

---

## 11.7 文案节流与去噪

- 同类事件 10 分钟内不重复播报
- 聚合事件优先于单一事件
- 下班后只保留时间类文案

---

## 11.8 技术实现建议

### 11.8.1 世界事件数据结构

```ts
interface WorldEvent {
  type: string;
  source: "time" | "tapd";
  level: "positive" | "neutral" | "negative";
  text: string;
  timestamp: number;
}
```

---

### 11.8.2 文案生成器接口

```ts
generateCopy(eventType): string
```

---

## 11.9 世界事件 & 文案 JSON 配置模板（工程用）

> 本节提供**可直接用于代码中的 JSON 配置模板**，用于驱动世界事件系统与文案生成器。所有文案均通过配置维护，避免硬编码。

---

### 11.9.1 世界事件定义（world-events.json）

```json
{
  "BUG_CREATED": {
    "id": "BUG_CREATED",
    "source": "tapd",
    "category": "status",
    "emotion": "neutral",
    "priority": 2,
    "cooldown": 600,
    "copyPool": ["🐞 检测到新的异常节点", "🧩 世界中出现了一些不稳定因素", "🛠 新的问题被世界感知到了"]
  },

  "BUG_FIXED": {
    "id": "BUG_FIXED",
    "source": "tapd",
    "category": "status",
    "emotion": "positive",
    "priority": 3,
    "cooldown": 300,
    "copyPool": ["🗡 异常节点已清除，世界线趋于稳定", "✨ 世界恢复了一部分秩序", "🧯 潜在风险已被妥善处理"]
  },

  "BUG_REOPENED": {
    "id": "BUG_REOPENED",
    "source": "tapd",
    "category": "status",
    "emotion": "negative",
    "priority": 4,
    "cooldown": 900,
    "copyPool": ["☠ 异常节点重新活跃，但尚可控制", "🔁 世界线出现回弹现象", "🌀 稳定性受到轻微挑战"]
  }
}
```

字段说明：

- `emotion`：情绪权重（positive / neutral / negative，仅允许轻负向）
- `priority`：事件优先级（聚合 > 状态 > 时间）
- `cooldown`：同类事件最短触发间隔（秒）
- `copyPool`：随机文案池

---

### 11.9.2 需求世界事件定义（story-events.json）

```json
{
  "STORY_CREATED": {
    "emotion": "neutral",
    "priority": 2,
    "copyPool": ["📜 新的剧情分支被写入世界", "🧭 世界线新增了一个发展方向"]
  },

  "STORY_IN_PROGRESS": {
    "emotion": "neutral",
    "priority": 1,
    "copyPool": ["⚙ 世界线正在稳定推进", "🛤 剧情执行中，一切按预期运转"]
  },

  "STORY_DONE": {
    "emotion": "positive",
    "priority": 3,
    "copyPool": ["✨ 一个阶段的故事暂告段落", "📖 剧情节点顺利收束"]
  },

  "STORY_ROLLBACK": {
    "emotion": "negative",
    "priority": 4,
    "copyPool": ["🔄 世界线发生回溯，但未脱轨", "🌀 剧情被调整，世界正在重新对齐"]
  }
}
```

---

### 11.9.3 时间类事件定义（time-events.json）

```json
{
  "TIME_STABLE": {
    "source": "time",
    "emotion": "positive",
    "priority": 0,
    "trigger": "online_30m",
    "copyPool": ["🟢 系统稳定运行中……", "🕰 时间正在缓慢流逝，一切如常"]
  },

  "TIME_LONG_SESSION": {
    "emotion": "neutral",
    "priority": 1,
    "trigger": "online_2h",
    "copyPool": ["🟡 长时间运行，注意能量消耗", "🔋 世界负载略高，但仍可持续"]
  },

  "TIME_OFFWORK": {
    "emotion": "positive",
    "priority": 5,
    "trigger": "offwork",
    "copyPool": ["🌙 世界进入低活跃状态", "🛌 挂机暂停，系统进入休眠模式"]
  }
}
```

---

### 11.9.4 聚合事件定义（aggregate-events.json）

```json
{
  "REQUIREMENT_STORM": {
    "condition": "story_change_count >= 3",
    "emotion": "neutral",
    "priority": 10,
    "copyPool": ["🌪 世界线受到外部扰动，正在自适应", "🌀 多重变量叠加，系统短暂波动"]
  },

  "SYSTEM_UNSTABLE": {
    "condition": "bug_reopen_count >= 2",
    "emotion": "negative",
    "priority": 10,
    "copyPool": ["🧯 异常频发，但仍在安全阈值内", "⚠ 系统稳定性受到考验，但未失控"]
  }
}
```

---

### 11.9.5 文案生成器读取规则（示意）

```ts
function generateCopy(eventId) {
  const event = eventConfig[eventId];
  return randomPick(event.copyPool);
}
```

> ❗UI 与业务逻辑 **不得直接拼接文案**，所有输出必须来自配置池。

---

## 十二、最终设计底线

- 不批评用户
- 不制造焦虑
- 不暗示个人能力问题

> 系统唯一要做的事： **陪你把这一天走完。**
